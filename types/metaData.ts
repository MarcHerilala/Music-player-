export type Metadata = {
        title?: string;
        artist?: string;
        album?: string;
        genre?:string;
        picture?: picture; 
}
 type picture={
    description: string;
    pictureData: string;
}